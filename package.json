{
  "name": "ecom-store",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node -e \"const fs=require('fs');['dist','.wrangler'].forEach(d=>{try{fs.rmSync(d,{recursive:true,force:true})}catch(e){}});\"",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.2.1",
    "astro": "^6.1.9"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
